services:
  - type: web
    name: metrixa-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 10000
